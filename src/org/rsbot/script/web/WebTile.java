package org.rsbot.script.web;

import org.rsbot.script.internal.wrappers.TileFlags;
import org.rsbot.script.wrappers.RSTile;

/**
 * A web tile handler.
 *
 * @author Timer
 */
public class WebTile extends TileFlags {
	public WebTile(RSTile tile, Integer[] keys) {
		super(tile, keys);
	}
}
